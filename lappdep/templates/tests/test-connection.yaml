apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lappdep.fullname" . }}-test-connection"
  labels:
    {{- include "lappdep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lappdep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
